[project]
name = "hubeau-data"
version = "0.3.1"
description = "Pythonic, typed, modern client for the Hubeau water data APIs."
authors = [{ name = "Pierre Feilles", email = "pierre.feilles@gmail.com" }]
readme = "README.md"
license = "MIT"
requires-python = ">=3.13"
keywords = ["hubeau", "water", "hydrology", "france", "open-data", "api-client"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Hydrology",
    "Typing :: Typed",
]
dependencies = [
    "httpx>=0.28.1",
    "pydantic>=2.7.0",
    "tenacity>=9.1.4",
]

[project.urls]
Homepage = "https://github.com/pfei/hubeau-data"
Repository = "https://github.com/pfei/hubeau-data"
Changelog = "https://github.com/pfei/hubeau-data/blob/main/CHANGELOG.md"

[project.optional-dependencies]
dataframe = [
    "pandas>=2.2.0",
]
geo = [
    "geopandas>=1.0.1",
    "shapely>=2.1.1",
]
viz = [
    "matplotlib>=3.10.3",
]
all = [
    "hubeau-data[dataframe,geo,viz]",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "ruff>=0.11.10",
    "pyright>=1.1.390",
    "notebook>=7.2.0",
    "jupyter>=1.1.1",
    "ipython>=8.24",
    "rich>=14.0.0",
    "tqdm>=4.67.1",
    "types-tabulate>=0.9.0.20241207",
    "pytest-httpx>=0.36.2",
    "pre-commit>=4.6.0",
    "python-lsp-server>=1.14.0",
    "jedi-language-server>=0.47.0",
    "pydantic>=2.13.4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hubeau_data"]

[tool.pytest.ini_options]
pythonpath = ["src"]
markers = [
    "live: Real API integration tests that interact with external networks",
]

[tool.ruff]
line-length = 88
src = ["src"]
extend-include = ["*.ipynb"]

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.ruff.lint.per-file-ignores]
"*.ipynb" = ["E501", "I001"]

[tool.ruff.lint.isort]
known-first-party = ["hubeau_data"]

[tool.pyright]
pythonVersion = "3.13"
typeCheckingMode = "strict"
venvPath = "."
venv = ".venv"

